```python
from functools import cmp_to_key

def solution(numbers):
    numbers = list(map(str, numbers))
    numbers.sort(key=cmp_to_key(compare))
    answer = ''.join(numbers)

    return "0" if answer[0] == "0" else answer

def compare(a, b):
    if a  + b > b + a:
        return -1
    elif a + b < b + a:
        return 1

    else:
        return 0
```
> **배운점 & 느낀점**
	- cmp_to_key 라는 모듈을 알게 되었는데, 이건 정렬 시 복잡한 정렬 기준을 만들 때 사용한다.
